rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Portfolio read access for everyone
    // Write access for projects, skills, and experience
    match /{document=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
